<main class="user-posts">
	<section class="user-preview"  id=<%= "user_id_#{@user.id}" %>>
		<%= render "users/user_tag", user: @user %>
	</section>
	<section class="all-posts">
		<h2>Posts (<%= @user.posts.count %>)</h2>
		<% @posts.order(updated_at: :desc).each do |post| %>
				<%= render "posts/post_preview", post: post %>
				<div class="recent-comments">
					<% post.recent_comments.each do |comment| %>
						<%= render "posts/comment", comment: comment %>
					<% end %>
				</div>
			</article>
		<% end %>
		<%= paginate @posts %>
	</section>
</main>
