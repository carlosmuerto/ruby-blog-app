<main class="all-users">
	<h1>All Users</h1>
	<% @users.each do |user| %>
		<section class="user-preview"  id=<%= "user_id_#{user.id}" %>>
			<%= render "users/user_tag", user: user %>
		</section>
	<% end %>
</main>
